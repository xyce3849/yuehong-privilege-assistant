ext {
    api_version_major = 1
    api_version_minor = 0
    api_version_patch = 3
    api_version_name = "${api_version_major}.${api_version_minor}.${api_version_patch}"
    api_version_code = api_version_major * 100 + api_version_minor * 10 + api_version_patch
}
